class Operador {
  constructor(operadorId, nombre, siglas, color, urlIcono) {
    this.operadorId = operadorId;
    this.nombre = nombre;
    this.siglas = siglas;
    this.color = color;
    this.urlIcono = urlIcono;
  }
}